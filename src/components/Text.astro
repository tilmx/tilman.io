---
interface Props {
	serif?: boolean;
	size?: "small" | "regular" | "large" | "huge";
}
const { serif, size } = Astro.props;
---

<p class:list={[size, { serif }]}><slot /></p>
<style is:global lang="scss">
	body {
		line-height: 1.2;
		font-family: "Space Grotesk", sans-serif;
		font-size: 20px;

		@media (max-width: 540px) {
			font-size: 18px;
		}
	}
</style>
<style lang="scss">
	p {
		margin: 0;
	}

	p.serif {
		font-family: "EB Garamond", serif;
	}

	p.small {
		font-size: 12px;
	}

	p.large {
		font-size: 36px;
	}

	p.huge {
		font-size: 48px;
	}

	@media (max-width: 960px) {
		p.huge {
			font-size: 40px;
		}
	}

	@media (max-width: 540px) {
		p.huge {
			font-size: 30px;
		}

		p.large {
			font-size: 26px;
		}
	}
</style>
